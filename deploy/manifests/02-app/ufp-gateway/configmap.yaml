# ============================================================
# ufp-gateway-service — ConfigMap
# 网关路由、Nacos、Sentinel 非敏感配置
# ============================================================
apiVersion: v1
kind: ConfigMap
metadata:
  name: ufp-gateway-config
  namespace: ${K8S_NAMESPACE}
  labels:
    app: ufp-gateway
data:
  SPRING_PROFILES_ACTIVE: "${SPRING_PROFILES_ACTIVE}"
  NACOS_SERVER_ADDR: "${NACOS_HOST}:${NACOS_PORT}"
  NACOS_NAMESPACE: "${NACOS_NAMESPACE}"
  SPRING_CLOUD_NACOS_DISCOVERY_ENABLED: "${NACOS_DISCOVERY_ENABLED}"
  JAVA_OPTS: >-
    -XX:+UseZGC
    -XX:MaxRAMPercentage=75.0
    -XX:+ExitOnOutOfMemoryError
    -Djava.security.egd=file:/dev/./urandom
    -Duser.home=/tmp
    -Dcsp.sentinel.log.dir=/tmp
