# ============================================================
# ZhiYu-Backend — ServiceAccount
# 最小权限，为未来 Alibaba Cloud IRSA 做准备
# IRSA 允许 Pod 通过 RAM Role 访问 OSS/SMS 而无需静态 AK/SK
# ============================================================
apiVersion: v1
kind: ServiceAccount
metadata:
  name: zhiyu-backend
  namespace: ${K8S_NAMESPACE}
  labels:
    app: zhiyu-backend
